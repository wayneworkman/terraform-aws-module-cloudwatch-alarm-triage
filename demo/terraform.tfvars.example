# Demo Configuration
# Copy this file to terraform.tfvars and customize for your environment

# Email address for alarm notifications
notification_email = "your-email@example.com"

# Prefix for all demo resources (change to avoid conflicts)
prefix = "triage-demo"

# Enable/disable the failing Lambda (set to false to stop demo)
enable_demo_failures = true